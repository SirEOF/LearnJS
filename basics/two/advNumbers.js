/* Number methods */

// Converts hexadecimal to decimal if starts with 0x
console.log(0xfff)

// Converts binary to decimal if starts with 0b
console.log(0b10101010)

// Converts ocatal to decimal if starts with 0o
console.log(0o1234567)


var num = 20;
var x = Infinity;
console.log(Number.isFinite(num));
console.log(Number.isNaN(num));
console.log(Number.isInteger(Infinity));
console.log(Number.isFinite(x))

